# Plan：更新项目文档（对齐当前实现）

## Summary
- 目标：仅更新根目录文档（README.md / PROJECT.md / PROGRESS.md）使其与当前实现一致，并以 LightRAG 模式作为 README 默认启动路线。
- 约束：不改代码、不引入新依赖；文档中不写入任何真实密钥/密码，统一强调使用环境变量或本地 profile 文件。

## Current State（已确认现状与偏差）
- [README.md](file:///e:/code/repository/ContractLens/README.md)
  - 仍提到 `application-example.yml`、`docker-compose up -d`、`SPEC.md`、`knowledge/`、`scripts/`，与当前仓库结构不一致。
  - 仍把向量数据库写成 “Chroma / Qdrant”，但项目实际配置与实现只使用 Chroma。
- [PROJECT.md](file:///e:/code/repository/ContractLens/PROJECT.md)
  - 架构/GraphRAG 章节包含 Microsoft GraphRAG（Python 离线构建）与复杂实体关系抽取等描述，但当前实现是：
    - legacy：Chroma + Neo4j（基于 `knowledge_docs` 的结构化字段构图与图谱上下文拼接）
    - lightrag：后端调用 LightRAG Server `/query` 获取上下文（不依赖 Neo4j）
  - 向量库选型、图谱实体/关系等部分与当前代码不完全匹配，容易误导。
- [PROGRESS.md](file:///e:/code/repository/ContractLens/PROGRESS.md)
  - 当前内容较贴近实现（SSE、双模式 RAG、status 可观测等），但需要与 README/PROJECT 的“默认启动方式/依赖”表述一致。

## Decisions（已确认）
- 文档范围：仅根文档（README / PROJECT / PROGRESS）。
- README 默认启动路线：LightRAG 模式（Neo4j 作为 legacy 可选）。

## Proposed Changes

### 1) README.md（面向“快速上手”与真实目录结构）
- **环境要求**
  - 默认路线：Java 17、Node 18、MySQL 8、LightRAG Server、（可选）Chroma/Neo4j（仅 legacy 需要）。
- **配置方式**
  - 移除不存在的 `application-example.yml` 拷贝步骤；
  - 改为推荐环境变量（示例项）：
    - `SPRING_DATASOURCE_URL/USERNAME/PASSWORD`
    - `JWT_SECRET`
    - `DASHSCOPE_API_KEY`（强调不要写入仓库）
    - `CONTRACTLENS_RAG_MODE=lightrag`
    - `LIGHTRAG_BASE_URL` / `LIGHTRAG_QUERY_PATH`
  - 补充 legacy 可选配置：
    - `CONTRACTLENS_RAG_MODE=legacy`
    - `CHROMA_URL`、`NEO4J_URI/USERNAME/PASSWORD`
- **启动方式**
  - 用“本地启动”替代 docker-compose（仓库未提供 compose 文件）：
    - 启动 MySQL / LightRAG（与可选 Chroma/Neo4j）
    - 后端 `mvn spring-boot:run`（若无 mvn，注明用 IDE 运行）
    - 前端 `npm install && npm run dev`
- **项目结构**
  - 用真实结构替换 `SPEC.md/knowledge/scripts`：
    - `PROJECT.md`
    - `PROGRESS.md`
    - `.trae/rules/spec.md`（开发规范入口）
    - `lightrag/inputs/`（示例/导出 inputs 目录）
- **文档链接**
  - 将“开发规范”链接指向 `.trae/rules/spec.md`（避免指向不存在的 `SPEC.md`）。

### 2) PROJECT.md（面向“架构说明”，去误导、贴近当前实现）
- **技术栈/组件**
  - 移除未落地的选型（例如 Microsoft GraphRAG Python 离线构建、Milvus/Qdrant 等）；
  - 明确当前两种 RAG 模式：
    - legacy（Chroma + Neo4j）：向量检索 + 图谱上下文（基于结构化字段）
    - lightrag（LightRAG Server）：后端 HTTP 调用 `/query` 获取上下文（不依赖 Neo4j）
- **数据源与索引**
  - 明确 `knowledge_docs` 是知识源；
  - `/api/knowledge/rebuild` 在不同模式的行为：
    - legacy：向量 ingest + Neo4j upsert
    - lightrag：导出 inputs 目录（由 LightRAG Server 负责索引）
- **分析链路**
  - 加入 SSE 对话链路的关键事件（status/answer/done/error）与“结构化结果落库 + 分段返回”机制说明（避免误解）。
- **删改章节策略**
  - 删除/缩减与当前实现不符的“实体/关系类型大全”“社区发现/全局搜索”等内容；
  - 保留“租房合同风险点”与产品定位等高层内容。

### 3) PROGRESS.md（轻量对齐）
- 调整措辞，使其与 README 默认路线一致（默认 LightRAG，legacy/Neo4j 作为可选）。
- 增补一条“文档更新/清理”的进度记录（若当前表述与 README/PROJECT 存在冲突则以 README/PROJECT 为准同步修正）。

## Verification
- 自检三项一致性：
  - README 的目录结构与实际仓库一致（不再出现不存在路径）
  - README/PROJECT 的默认启动路线一致（LightRAG）
  - 不包含任何真实 API key/密码示例值
- 前端 `npm build`（可选）与 IDE diagnostics（可选）不受文档改动影响。

