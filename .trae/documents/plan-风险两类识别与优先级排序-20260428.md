## Summary
- 目标：在一次分析中同时输出两类风险：①合同现有条款风险；②合同未提及/缺失项风险，并把两类风险按优先级整体排序（若超长则仅输出优先级最高的前几条）。
- 展示策略（已确认）：文本答案中以单列表连续编号展示，并在每条风险标题前加【现有】/【缺失】标记；结构化 JSON 不新增字段，继续用 `clause_index=0` + `clause_text` 以“【缺失】”开头表示缺失项。

## Current State Analysis
- LLM 侧：提示词已要求识别“缺失项风险”，并约定缺失项编码（`clause_index=0`、`clause_text` 以“【缺失】”开头），且每个视角最多 5 条风险、按优先级排序：
  - [AiContractAnalyst.analyzeContract](file:///e:/code/repository/ContractLens/contractlens-backend/src/main/java/com/contractlens/service/ai/AiContractAnalyst.java#L11-L89)
  - [AiContractAnalyst.analyzeContractConcise](file:///e:/code/repository/ContractLens/contractlens-backend/src/main/java/com/contractlens/service/ai/AiContractAnalyst.java#L91-L119)
- 后端文本答案：目前仅按数组顺序输出风险，未显式标记“现有/缺失”，用户无法一眼区分两类风险。
  - [AnalysisService.appendRiskSection](file:///e:/code/repository/ContractLens/contractlens-backend/src/main/java/com/contractlens/service/AnalysisService.java#L476-L509)
- 结构化结果：前端风险卡片会直接展示 `risk_type/risk_level/clause_text...`，如果缺失项以“【缺失】”开头，前端可天然显示该前缀，但标题仍缺少“现有/缺失”的显式分类提示。
  - [ClauseCard.vue](file:///e:/code/repository/ContractLens/contractlens-frontend/src/components/ClauseCard.vue#L1-L30)

## Proposed Changes

### 1) 提示词：强制“两类风险都要产出 + 统一按优先级混排”
**文件**：`contractlens-backend/src/main/java/com/contractlens/service/ai/AiContractAnalyst.java`
- 在【分析要求】或【数组要求】中补充硬约束：
  - `party_*_risks` 必须同时包含【现有条款风险】与【缺失项风险】（只要能识别到缺失项）。
  - 两类风险必须“统一按优先级”混排在同一个数组中（不得把缺失项全部放最后）。
  - 若信息不足以判断缺失项：允许缺失项为空数组，但需优先输出现有条款风险的 Top N（仍按优先级）。
- 保持既有缺失项编码（`clause_index=0` + `clause_text` 以“【缺失】”开头），不新增 JSON 字段，避免后端解析与前端展示结构变更。

### 2) 文本答案：单列表连续编号 + 【现有】/【缺失】标记
**文件**：`contractlens-backend/src/main/java/com/contractlens/service/AnalysisService.java`
- 在 `appendRiskSection(...)` 中：
  - 识别每条风险来源：
    - 缺失项：`clause_index == 0` 或 `clause_text` 以“【缺失】”开头
    - 否则视为现有条款风险
  - 输出标题格式调整为：`{index}. 【现有/缺失】{risk_type}（{risk_level}）`
  - 保持单列表连续编号与原先输出顺序（即 LLM 已按优先级排序后的顺序），实现“整体优先级排序 + 清晰分类”同时成立。

### 3) 测试补齐
**文件**：新增 `contractlens-backend/src/test/java/com/contractlens/service/AnalysisServiceRiskClassificationTest.java`
- 覆盖：
  - 当风险条目 `clause_index=0` 或 `clause_text` 以“【缺失】”开头时，文本答案标题包含“【缺失】”标记
  - 其他条目标题包含“【现有】”标记
  - 仍保持连续编号与顺序不变

### 4) 进度记录
**文件**：`PROGRESS.md`
- 追加/更新“风险输出可区分现有/缺失且统一按优先级排序”的已完成记录（不改动功能边界）。

## Assumptions & Decisions
- 决策：结构化 JSON 不新增字段；缺失项继续用 `clause_index=0` + `clause_text` 前缀“【缺失】”标识。
- 决策：文本答案采用“单列表 + 标记”方式，同时满足“两类风险可区分”和“整体按优先级排序”。
- 假设：LLM 输出的 `party_*_risks` 已按优先级排序；后端不再做二次排序（仅加标记）。

## Verification
- 静态检查：IDE diagnostics 无新增错误。
- 单测：新增测试通过（重点验证标记逻辑与格式输出）。
- 手工验收：
  - 对同一合同做初次分析：
    - 租客/房东视角风险列表每条都带【现有】或【缺失】标记
    - 两类风险在同一个列表内按优先级混排（缺失项不应被固定塞到列表末尾）
    - 若超过最大条数，仅展示优先级最高的前几条（当前上限 5 条/视角）

