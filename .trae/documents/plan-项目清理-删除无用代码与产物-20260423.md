# 项目清理计划（删除无用代码与产物）

## Summary
- 目标：删除确定未使用/无效的代码与仓库内不应版本化的产物（dist/venv/chroma sqlite），同时清理根目录一次性脚本与依赖文件。
- 约束：按你的选择“仅删无用代码”为主，避免大规模重构；但会执行“移除仓库产物并补齐 .gitignore”。

## Current State Analysis（基于仓库现状）
- 前端：
  - `contractService.analyzeContract()` 未被任何地方调用，仅保留定义：[contract.js](file:///e:/code/repository/ContractLens/contractlens-frontend/src/api/contract.js#L124-L127)。
  - Dashboard 欢迎语引用 `authStore.username`，但 auth store 里不存在 `username` 字段，属于无效引用：[Dashboard.vue](file:///e:/code/repository/ContractLens/contractlens-frontend/src/views/Dashboard.vue#L33) / [auth.js](file:///e:/code/repository/ContractLens/contractlens-frontend/src/stores/auth.js#L4-L27)。
  - Dashboard 有遗留 `console.error`（调试噪音）：[Dashboard.vue](file:///e:/code/repository/ContractLens/contractlens-frontend/src/views/Dashboard.vue#L252-L259)。
- 后端：
  - LightRAG 健康检查方法未接入任何 controller/service，属于闲置代码：`LightRagClient.checkHealth()` + `LightRagHealthResult`。[LightRagClient.java](file:///e:/code/repository/ContractLens/contractlens-backend/src/main/java/com/contractlens/service/lightrag/LightRagClient.java#L32-L48) / [LightRagHealthResult.java](file:///e:/code/repository/ContractLens/contractlens-backend/src/main/java/com/contractlens/service/lightrag/LightRagHealthResult.java)
- 仓库产物：
  - `contractlens-frontend/dist/` 已存在（构建产物不应入库）。
  - `venv/` 已存在（本地 Python 虚拟环境不应入库）。
  - `chroma/chroma.sqlite3` 已存在（本地运行数据不应入库）。
  - 根目录存在一次性脚本与其依赖：`midterm_ppt.js`、根 `package.json`、根 `package-lock.json`（与主前后端无直接耦合）。
  - `.gitignore` 缺少忽略 `contractlens-frontend/dist/`，且误写了 `contractlens-backend/dist/`：[.gitignore](file:///e:/code/repository/ContractLens/.gitignore#L12-L16)。

## Proposed Changes

### 1) Frontend：删除无用/无效引用（最小改动）
- 文件：[contract.js](file:///e:/code/repository/ContractLens/contractlens-frontend/src/api/contract.js)
  - 删除 `contractService.analyzeContract(contractId)`（未使用）。
- 文件：[Dashboard.vue](file:///e:/code/repository/ContractLens/contractlens-frontend/src/views/Dashboard.vue)
  - 移除 `authStore.username` 的显示逻辑，欢迎语改为不依赖 username（按你选择“删除显示”）。
  - 删除/替换 `console.error("Fetch failed", err)`，用页面已有的错误 banner（`uploadError`）承接（避免控制台噪音）。

### 2) Backend：删除闲置 LightRAG 健康检查代码
- 文件：[LightRagClient.java](file:///e:/code/repository/ContractLens/contractlens-backend/src/main/java/com/contractlens/service/lightrag/LightRagClient.java)
  - 删除 `checkHealth()` 方法（当前无调用链）。
- 文件：[LightRagHealthResult.java](file:///e:/code/repository/ContractLens/contractlens-backend/src/main/java/com/contractlens/service/lightrag/LightRagHealthResult.java)
  - 删除整个文件（仅服务于 `checkHealth()`，删除后无意义）。

### 3) 仓库清理：移除不应入库的产物 + 补齐忽略规则
- 删除并停止跟踪：
  - `contractlens-frontend/dist/`
  - `venv/`
  - `chroma/chroma.sqlite3`（必要时连同 `chroma/` 下其余运行时文件一起）
- 根目录删除（按你选择）：
  - `midterm_ppt.js`
  - 根 `package.json`
  - 根 `package-lock.json`
- 更新 [.gitignore](file:///e:/code/repository/ContractLens/.gitignore)
  - 增加：`contractlens-frontend/dist/`、`venv/`、`chroma/*.sqlite3`（或 `chroma/`）
  - 移除或修正：`contractlens-backend/dist/`（若为误写则删除）

### 4) 文档同步
- 更新 [PROGRESS.md](file:///e:/code/repository/ContractLens/PROGRESS.md)：
  - 记录本次“清理：移除构建/运行产物 + 删除无用代码”的变更点（不扩写功能描述）。

## Assumptions & Decisions
- 不做深度重构：不抽取重复 `blankToNull`、不合并 Knowledge/Settings 的重复 computed（避免扩大改动面）。
- 不改动现有 LightRAG/Legacy/Neo4j 的业务分支逻辑，只移除确定无引用的健康检查代码。
- 产物移除属于“项目清理”范围的一部分，按你的选择执行。

## Verification
- 前端：
  - `npm.cmd -C contractlens-frontend run build` 通过。
  - 全仓检索确认删除方法/字段无残留引用（例如 `authStore.username`、`analyzeContract(`、`checkHealth(`、`LightRagHealthResult`）。
- 后端：
  - IDE Diagnostics 无编译错误（如当前环境无法运行 `mvn`，以诊断为准）。
- 仓库：
  - 目标目录/文件已删除，`.gitignore` 已覆盖，避免再次进入版本库。

